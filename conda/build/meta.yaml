{% set name = "arx" %}
{% set version = "1.1.1" %}  # semantic-release
{% set llvm_version = "13.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  git_url: {{git_url}}  # note: managed by make
  git_tag: {{git_branch}}  # note: managed by make
  git_depth: 1

build:
  number: 0
  skip: True  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake >=3.21
    - ninja
    - sysroot_linux-64 2.17  # [linux64]
  host:
    - arrow-cpp 7.*
    - clangdev {{ llvm_version }}
    - libclang-cpp {{ llvm_version }}
    - lld {{ llvm_version }}
    - llvm {{ llvm_version }}
    - llvmdev {{ llvm_version }}
  run:
    - arrow-cpp 7.*

test:
  commands:
    - arx --version

about:
  home: https://github.com/arx-org/arx
  license: Apache-2.0
  license_family: Apache
  summary: Arx compiler aims to provide arrow datatypes as native datatypes.
  description: |
    Arx compiler aims to provide arrow datatypes as native datatypes.
    For now, it is strongly based on Kaleidoscope compiler with a few changes.
    **ROADMAP**
    * Implement more datatypes
      * int8, int16, int32, int64
      * float16, float32, float64
      * string
      * datetime
    * Change the language design (syntax)
    * Implement arrow datatypes as native datatypes
  doc_url: https://github.com/arx-org/arx
  dev_url: https://github.com/arx-org/arx

extra:
  recipe-maintainers:
    - xmnlab
